# Contracts

Client-only pseudo-API contracts describing in-browser export behaviors.

- `export-openapi.yaml`: Documents CSV and HTML table export payloads and responses. Implemented entirely client-side (no network calls).
